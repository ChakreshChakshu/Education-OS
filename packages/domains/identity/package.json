{
  "name": "@eos/domain-identity",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "echo \"identity domain build: ok\"",
    "lint": "eslint **/*.js",
    "test": "node --test test/identity.test.js"
  },
  "dependencies": {
    "@eos/core": "workspace:*"
  }
}
